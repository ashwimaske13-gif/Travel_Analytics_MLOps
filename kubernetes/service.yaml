apiVersion: v1
kind: Service
metadata:
  name: travel-analytics-service
spec:
  type: NodePort
  selector:
    app: travel-analytics
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080